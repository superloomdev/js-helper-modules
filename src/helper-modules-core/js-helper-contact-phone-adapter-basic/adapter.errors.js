'use strict';

/**
 * Error catalog for helper-contact-phone-adapter-basic.
 * The basic adapter is stateless and does not produce operational errors.
 * The catalog exists per the Universal Companion Files rule.
 */

module.exports = Object.freeze({});
