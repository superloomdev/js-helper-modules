// Info: Error catalog for helper-contact-phone-adapter-basic.
// The basic adapter delegates reason codes to the core's catalog.
// This catalog is kept for structural conformance (Universal Companion Files).
// Frozen to prevent accidental mutation.

export default Object.freeze({

  ADAPTER_ERROR: Object.freeze({
    type: 'CONTACT_PHONE_ADAPTER_BASIC_ERROR',
    message: 'Basic phone adapter encountered an error'
  })

});
