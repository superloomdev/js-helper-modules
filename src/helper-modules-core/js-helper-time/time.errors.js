// Info: Error catalog for helper-time. Currently empty - Time has no
// domain-specific error definitions. All functions are pure and return
// runtime defaults (NaN, Invalid Date) for unparseable input rather than
// throwing domain errors.
//
// Frozen to prevent accidental mutation.
'use strict';


module.exports = Object.freeze({

  // No error definitions - Time functions return runtime defaults
  // for bad input rather than throwing domain errors.

});
