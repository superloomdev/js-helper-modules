// Info: Error catalog for helper-contact-phone-adapter-extended.
// The extended adapter delegates reason codes to the core's catalog.
// This catalog is kept for structural conformance (Universal Companion Files).
// Frozen to prevent accidental mutation.
'use strict';

module.exports = Object.freeze({

  ADAPTER_ERROR: Object.freeze({
    type: 'CONTACT_PHONE_ADAPTER_EXTENDED_ERROR',
    message: 'Extended phone adapter encountered an error'
  })

});
