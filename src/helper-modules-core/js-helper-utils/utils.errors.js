// Info: Error catalog for helper-utils. Currently empty - Utils has no
// domain-specific error definitions. All failures are programmer errors
// (wrong type passed) which throw TypeError, not domain errors.
//
// Singleton: Loaded once at require time by the main module. Frozen to
// prevent accidental mutation.
'use strict';


module.exports = Object.freeze({

  // No error definitions yet - Utils has no domain-specific errors.
  // Add error definitions here as the module grows.

});
