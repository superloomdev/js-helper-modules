# DynamoDB Local for cache-store-dynamodb module testing
# Lifecycle managed by npm scripts: npm test starts -> tests -> stops and removes
# Ephemeral - data lost on container stop
# Port 8002 to avoid conflict with nosql-aws-dynamodb test on 8000
# and auth-store-dynamodb test on 8001

name: superloom-test-cache-store-dynamodb

services:

  dynamodb:
    image: amazon/dynamodb-local:latest
    container_name: superloom-test-cache-store-dynamodb
    ports:
      - "127.0.0.1:8002:8000"
    command: ["-jar", "DynamoDBLocal.jar", "-sharedDb"]
    healthcheck:
      test: ["CMD-SHELL", "curl -s -o /dev/null http://localhost:8000 2>/dev/null; [ $? -ne 7 ]"]
      interval: 2s
      timeout: 3s
      retries: 15
      start_period: 5s
