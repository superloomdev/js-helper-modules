// Info: Configuration file for helper-storage-local-fs


export default {

  // Root directory for all file operations. All bucket and key paths are
  // resolved relative to this directory. Path traversal outside this root
  // is blocked at resolve time.
  ROOT_DIRECTORY: './storage'

};
