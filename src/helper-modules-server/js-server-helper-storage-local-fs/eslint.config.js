// Info: ESLint flat config for js-server-helper-storage-local-fs. Delegates
// to the shared @superloomdev/js-helper-eslint-config package via the `esm`
// preset. No per-module rule overrides are permitted.
import { esm } from '@superloomdev/js-helper-eslint-config';

export default esm;
